{
  "name": "@novu/thalamus-cloudflare",
  "version": "0.1.0",
  "description": "Cloudflare Worker — durable session observer with webhook delivery",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250424.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.14.0"
  },
  "dependencies": {
    "@novu/thalamus": "workspace:*",
    "agents": "^0.12.4",
    "eventsource-parser": "^3.0.8"
  }
}
